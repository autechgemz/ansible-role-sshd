---
- name: Install tasks
  become: true
  tags:
    - sshd_install
    - sshd
  block:
    - name: Ensure packages
      ansible.builtin.package:
        name: ["{{ item }}"]
        state: present
      with_items: "{{ sshd_package_name }}"
